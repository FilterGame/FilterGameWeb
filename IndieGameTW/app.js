/**********************
 * 語言切換功能
 **********************/
 
 	const translations = {
	  zh: {
		siteTitle: "FilterGame",
		metaDescription: "FilterGame 遊戲開發.",
		"nav.logo": "FilterGame ",
		"nav.home.title": "首頁 ▼",
		"nav.home.item1": "首頁",
		"nav.home.item2": "遊戲資訊",
		"nav.home.item3": "團隊介紹",
		"nav.game.title": "遊戲 ▼",
		"nav.game.item1": "AirBoost(2017)",
		"nav.game.item2": "AirBoost:天空機士",
		"nav.game.item3": "熱門遊戲",
		"nav.game.item4": "下載",
		"nav.planning.title": "企劃&宣傳 ▼",
		"nav.planning.item1": "IndieGameWebTW",
		"nav.planning.item2": "東亞遊戲特賣2025",
		"nav.planning.item3": "台灣遊戲夢-團隊介紹",
		"nav.contact.title": "聯絡我 ▼",
		"nav.contact.item1": "聯絡我們",
		"nav.contact.item2": "常見問題",
		"nav.contact.item3": "支援",
		"lang.en": "English",
		"lang.zh": "中文",
		"lang.ja": "日本語",
		"hero.title": "冒險",
		"section.popularGames.title": "開發遊戲列表",
		"section.popularGames.ingoogleplay": "GooglePlay商店 - 免費下載",
		"section.popularGames.demo": "DEMO開放中!",
		"section.popularGames.freeplay": "免費遊玩!",
		"section.popularGames.steamrelease": "Steam-2025發售預定",
		"section.latestImages.title": "最新圖片",
		"section.characters.title": "角色",
		"section.characters.button": "查看所有角色",
		"section.pic.title": "圖片創作",
		"section.contact.title": "聯絡我們",
		"section.contact.card.title": "聯絡我們",
		"section.contact.card.desc": "如有遊戲相關疑問，請聯絡我們",
		"footer.copyright": "FilterGame © 版權所有",
		"footer.site": "FilterGame - 遊戲開發",
		"expand": "展開",
		"news_item": "=====",
		"news_full_item_1": "=====",
		"news_item2": "=====",
		"news_full_item_2": "=====",
		"steam_airboost_title": "AirBoost:天空機士",
		"News": "最新消息",
		"section.com.title" : "FilterGame 遊戲開發團隊介紹 ＼(^ω^＼)",
		"section.com.info" : "FilterGame是台灣的獨立遊戲開發團隊,特別喜歡日本風格的動作遊戲!<br>除了有做遊戲外也會舉辦各種活動!希望能讓台灣獨立遊戲圈更好!<br>也正在開發Steam美少女動作遊戲<AirBoost:天空機士>!!",
		"nav.add.title" : "投稿▼",
		"nav.add.item1" : "註冊你的steam遊戲",
		"nav.add.item2" : "投稿彈慕訊息",
	  },

	  en: {
		siteTitle: "FilterGame",
		metaDescription: "FilterGame Game Development.",
		"nav.logo": "FilterGame ",
		"nav.home.title": "Home ▼",
		"nav.home.item1": "Home",
		"nav.home.item2": "Game Info",
		"nav.home.item3": "Team Introduction",
		"nav.game.title": "Games ▼",
		"nav.game.item1": "AirBoost(2017)",
		"nav.game.item2": "AirBoost Airship Knight",
		"nav.game.item3": "Popular Games",
		"nav.game.item4": "Download",
		"nav.planning.title": "Planning & Promotion ▼",
		"nav.planning.item1": "IndieGameWebTW",
		"nav.planning.item2": "East Asia Game Sale 2025",
		"nav.planning.item3": "Taiwan Game Dream - Team Intro",
		"nav.contact.title": "Contact ▼",
		"nav.contact.item1": "Contact Us",
		"nav.contact.item2": "FAQ",
		"nav.contact.item3": "Support",
		"lang.en": "English",
		"lang.zh": "Chinese",
		"lang.ja": "Japanese",
		"hero.title": "Adventure",
		"section.popularGames.title": "Developed Game List",
		"section.popularGames.ingoogleplay": "Google Play Store - Free Download",
		"section.popularGames.demo": "DEMO Available!",
		"section.popularGames.freeplay": "Free to Play!",
		"section.popularGames.steamrelease": "Steam - Releasing in 2025",
		"section.latestImages.title": "Latest Images",
		"section.characters.title": "Characters",
		"section.characters.button": "View All Characters",
		"section.pic.title": "Image Creations",
		"section.contact.title": "Contact Us",
		"section.contact.card.title": "Contact Us",
		"section.contact.card.desc": "For game-related questions, please contact us.",
		"footer.copyright": "FilterGame © All Rights Reserved",
		"footer.site": "FilterGame - Game Development",
		"expand": "Expand",
		"news_item": "=====",
		"news_full_item_1": "=====",
		"news_item2": "=====",
		"news_full_item_2": "=====",
		"steam_airboost_title": "AirBoost Airship Knight",
		"News": "News",
		"section.com.title" : "FilterGame Game Development Team Introduction ＼(^ω^＼)",
		"section.com.info" : "FilterGame is an indie game development team from Taiwan!<br>We especially love Japanese-style action games!In addition to making games, we also organize various events!Currently,<br>we’re developing a Steam game titled AirBoost Airship Knight!",
		"nav.add.title" : "Post▼",
		"nav.add.item1" : "Register your Steam game!",
		"nav.add.item2" : "Submit a bullet chat message!",
	  },

	  ja: {
		siteTitle: "FilterGame",
		metaDescription: "FilterGame ゲーム開発.",
		"nav.logo": "FilterGame ",
		"nav.home.title": "ホーム ▼",
		"nav.home.item1": "ホーム",
		"nav.home.item2": "ゲーム情報",
		"nav.home.item3": "チーム紹介",
		"nav.game.title": "ゲーム ▼",
		"nav.game.item1": "AirBoost(2017)",
		"nav.game.item2": "AirBoost エアシップの騎士",
		"nav.game.item3": "人気ゲーム",
		"nav.game.item4": "ダウンロード",
		"nav.planning.title": "企画＆宣伝 ▼",
		"nav.planning.item1": "IndieGameWebTW",
		"nav.planning.item2": "東アジアゲームセール2025",
		"nav.planning.item3": "台湾ゲーム夢・チーム紹介",
		"nav.contact.title": "お問い合わせ ▼",
		"nav.contact.item1": "お問い合わせ",
		"nav.contact.item2": "よくある質問",
		"nav.contact.item3": "サポート",
		"lang.en": "英語",
		"lang.zh": "中国語",
		"lang.ja": "日本語",
		"hero.title": "冒険",
		"section.popularGames.title": "開発ゲームリスト",
		"section.popularGames.ingoogleplay": "Google Playストア - 無料ダウンロード",
		"section.popularGames.demo": "DEMO公開中！",
		"section.popularGames.freeplay": "無料プレイ！",
		"section.popularGames.steamrelease": "Steam - 2025年発売予定",
		"section.latestImages.title": "最新画像",
		"section.characters.title": "キャラクター",
		"section.characters.button": "すべてのキャラを見る",
		"section.pic.title": "画像作品",
		"section.contact.title": "お問い合わせ",
		"section.contact.card.title": "お問い合わせ",
		"section.contact.card.desc": "ゲームに関するご質問はお気軽にご連絡ください。",
		"footer.copyright": "FilterGame © 全著作権所有",
		"footer.site": "FilterGame - ゲーム開発",
		"expand": "展開",
		"news_item": "=====",
		"news_full_item_1": "=====",
		"news_item2": "=====",
		"news_full_item_2": "=====",
		"steam_airboost_title": "	AirBoost エアシップの騎士",
		"News": "最新情報",
		"section.com.title" : "FilterGame ゲーム開発チーム紹介 ＼(^ω^＼)",
		"section.com.info" : "FilterGame は台湾のインディーゲーム開発チームです！<br>日本風のアクションゲームが大好き！ゲーム開発だけでなく、様々な活動も行っています！<br>現在は Steam向けタイトル『AirBoost エアシップの騎士』 を開発中です！",
		"nav.add.title" : "投稿▼",
		"nav.add.item1" : "あなたのSteamゲームを登録しよう！",
		"nav.add.item2" : "弾幕メッセージを投稿しよう！",
	  }
	};
 
let currentLanguage = "en";
const navLang = navigator.language.toLowerCase();
if (navLang.startsWith("zh")) {
  currentLanguage = "zh";
} else if (navLang.startsWith("ja")) {
  currentLanguage = "ja";
}
function clearDanmaku() {
  const container = document.getElementById("danmaku-container");
  if (container) {
    container.innerHTML = "";
  }
}
let currentCSVButton = null;
function updateCardTitles() {
  document.querySelectorAll(".game-title").forEach((h2) => {
    const newText = h2.getAttribute("data-" + currentLanguage);
    if (newText) {
      h2.textContent = newText;
    }
  });
  document.querySelectorAll("[data-navtext]").forEach((el) => {
    const newText = el.getAttribute("data-" + currentLanguage);
    if (newText) {
      if (el.id === "modal-content") {
        el.innerHTML = newText;
      } else {
        el.textContent = newText;
      }
    }
  });
  updatePageTitle();
}
function updatePageTitle() {
  if (currentCSVButton) {
    const titleText = currentCSVButton.getAttribute("data-" + currentLanguage);
    const pageTitleEl = document.getElementById("page-title");
    if (pageTitleEl && titleText) {
      pageTitleEl.textContent = titleText;
    }
  }
}
document.querySelectorAll(".lang-btn").forEach((btn) => {
  btn.addEventListener("click", function () {
    currentLanguage = this.getAttribute("data-lang");
    updateCardTitles();
  });
});


      function updateLanguage(lang) {
        document.querySelectorAll("[data-i18n]").forEach(el => {
          const key = el.getAttribute("data-i18n");
          if(key) {
            // 若屬性值以 [content] 開頭，代表更新 element 的屬性
            if(key.indexOf("[content]") === 0) {
              const realKey = key.replace("[content]", "");
              el.setAttribute("content", translations[lang][realKey] || "");
            } else {
              el.innerHTML = translations[lang][key] || "";
            }
          }
        });
        // 更新 <html> 的 lang 屬性
        document.documentElement.lang = lang;
      }


      // 當使用者切換語言時更新
      document.getElementById("langSelect").addEventListener("change", function() {
        updateLanguage(this.value);
		
		currentLanguage = this.value;
		updateCardTitles();

      });


/**********************
 * CSV 讀取及遊戲卡片建立（含防呆與隨機功能）
 **********************/
function loadCSV(csvFile) {
  const grid = document.getElementById("game-grid");
  grid.innerHTML = "";
  fetch(csvFile + "?v=" + Date.now())
    .then((response) => response.text())
    .then((text) => {
      const lines = text.trim().split("\n");
      let rows = [];
      for (let i = 1; i < lines.length; i++) {
        try {
          const row = lines[i].split(",");
          if (row.length < 4) {
            console.error(`第 ${i + 1} 行資料不足欄位，將跳過：${lines[i]}`);
            continue;
          }
          rows.push(row);
        } catch (error) {
          console.error(
            `處理第 ${i + 1} 行資料時發生錯誤，將跳過該行：${lines[i]}`,
            error
          );
          continue;
        }
      }
      if (
        currentCSVButton &&
        currentCSVButton.getAttribute("data-random") === "true"
      ) {
        rows.sort(() => Math.random() - 0.5);
      }
      rows.forEach((row, index) => {
        const steamLink = row[0].trim();
        const title_en = row[1].trim();
        const title_zh = row[2].trim();
        const title_ja = row[3].trim();
        const regex = /\/app\/(\d+)\//;
        const match = steamLink.match(regex);
        if (!match) {
          console.error(
            `第 ${index + 2} 行無法解析 Steam 連結，將跳過：${steamLink}`
          );
          return;
        }
        const appId = match[1];
        const imageUrl = `https://steamcdn-a.akamaihd.net/steam/apps/${appId}/library_600x900_2x.jpg`;
        const cardDiv = document.createElement("div");
        cardDiv.className = "game-card";
        const aTag = document.createElement("a");
        aTag.href = steamLink;
        aTag.target = "_blank";
        aTag.rel = "noopener noreferrer";
        aTag.className =
          "block overflow-hidden rounded-lg shadow-lg hover:shadow-xl transition-shadow duration-300";
        const innerDiv = document.createElement("div");
        innerDiv.className = "relative aspect-[3/4]";
        const imgTag = document.createElement("img");
        imgTag.src = imageUrl;
        imgTag.alt = title_en;
        imgTag.className = "object-cover w-full h-full";
        innerDiv.appendChild(imgTag);
        const overlayDiv = document.createElement("div");
        overlayDiv.className =
          "absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/60 to-transparent p-4";
        const h2 = document.createElement("h2");
        h2.className = "game-title text-white text-xl font-medium";
        h2.setAttribute("data-en", title_en);
        h2.setAttribute("data-zh", title_zh);
        h2.setAttribute("data-ja", title_ja);
        h2.textContent = h2.getAttribute("data-" + currentLanguage);
        overlayDiv.appendChild(h2);
        innerDiv.appendChild(overlayDiv);
        aTag.appendChild(innerDiv);
        cardDiv.appendChild(aTag);
        grid.appendChild(cardDiv);
      });
      const observer = new IntersectionObserver(
        (entries, obs) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add("in-view");
              obs.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.1 }
      );
      document.querySelectorAll(".game-card").forEach((card) => {
        observer.observe(card);
      });
    })
    .catch((error) => console.error("讀取 CSV 檔案發生錯誤:", error));
}

document.querySelectorAll(".csv-btn").forEach((btn) => {
  btn.addEventListener("click", function () {
    document.querySelectorAll(".csv-btn").forEach((btn) => {
      btn.style.backgroundColor = "#3b82f6";
    });
    this.style.backgroundColor = "red";
    currentCSVButton = this;
    const csvFile = this.getAttribute("data-csv");
    loadCSV(csvFile);
    updatePageTitle();
    clearDanmaku();
  });
});

window.addEventListener("DOMContentLoaded", () => {
  const firstCsvBtn = document.querySelector(".csv-btn");
  if (firstCsvBtn) {
    firstCsvBtn.style.backgroundColor = "red";
    currentCSVButton = firstCsvBtn;
    loadCSV(firstCsvBtn.getAttribute("data-csv"));
    updatePageTitle();
  }
  // 啟動彈幕功能
  loadDanmakuCSV();
});

/**********************
 * Modal (更新履歷&說明) 功能
 **********************/
const modalOverlay = document.getElementById("modal-overlay");
const updateModalBtn = document.getElementById("update-modal-btn");
updateModalBtn.addEventListener("click", function (e) {
  e.preventDefault();
  modalOverlay.classList.add("active");
});
modalOverlay.addEventListener("click", function () {
  modalOverlay.classList.add("closing");
  setTimeout(() => {
    modalOverlay.classList.remove("active");
    modalOverlay.classList.remove("closing");
  }, 500);
});

/**********************
 * 彈幕功能
 **********************/
let danmakuList = [];
let totalDanmakuWeight = 0;
let danmakuIntervalId = null;
let danmakuEnabled = true; // 新增變數來追蹤彈幕狀態

function loadDanmakuCSV() {
  fetch("danmaku.csv?v=" + Date.now())
    .then((response) => response.text())
    .then((text) => {
      const lines = text.trim().split("\n");
      danmakuList = [];
      totalDanmakuWeight = 0;
      for (let i = 1; i < lines.length; i++) {
        const parts = lines[i].split(",");
        if (parts.length < 2) {
          console.error(`彈幕 CSV 第 ${i + 1} 行資料不足，跳過：${lines[i]}`);
          continue;
        }
        const message = parts[0].trim();
        const weight = parseFloat(parts[1]);
        if (isNaN(weight) || weight <= 0) continue;
        danmakuList.push({ message, weight });
        totalDanmakuWeight += weight;
      }
      startDanmakuInterval();
    })
    .catch((error) => console.error("讀取彈幕 CSV 發生錯誤:", error));
}

function chooseDanmaku() {
  if (danmakuList.length === 0) return null;
  let r = Math.random() * totalDanmakuWeight;
  for (const item of danmakuList) {
    r -= item.weight;
    if (r <= 0) return item.message;
  }
  return danmakuList[danmakuList.length - 1].message;
}

function createDanmaku(message) {
  const container = document.getElementById("danmaku-container");
  if (!container) return;
  const danmakuEl = document.createElement("div");
  danmakuEl.className = "danmaku-item";
  danmakuEl.textContent = message;
  danmakuEl.style.top = Math.random() * 90 + "%";
  container.appendChild(danmakuEl);
  danmakuEl.addEventListener("animationend", () => {
    danmakuEl.remove();
  });
}

function startDanmakuInterval() {
  stopDanmakuInterval();
  if (danmakuEnabled) {
    danmakuIntervalId = setInterval(() => {
      const msg = chooseDanmaku();
      if (msg) {
        createDanmaku(msg);
      }
    }, 4800);
  }
}

function stopDanmakuInterval() {
  if (danmakuIntervalId !== null) {
    clearInterval(danmakuIntervalId);
    danmakuIntervalId = null;
  }
}

document.addEventListener("visibilitychange", () => {
  if (document.hidden) {
    stopDanmakuInterval();
  } else {
    clearDanmaku();
    startDanmakuInterval();
  }
});

/**********************
 * 彈幕開關按鈕事件處理
 **********************/
const danmakuToggleBtn = document.getElementById("danmaku-toggle");
danmakuToggleBtn.addEventListener("click", () => {
  danmakuEnabled = !danmakuEnabled;
  if (danmakuEnabled) {
    danmakuToggleBtn.textContent = "彈幕 ON";
    startDanmakuInterval();
  } else {
    danmakuToggleBtn.textContent = "彈幕 OFF";
    stopDanmakuInterval();
    clearDanmaku();
  }
});
